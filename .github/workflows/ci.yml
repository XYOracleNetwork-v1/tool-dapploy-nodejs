name: CI

on:
  push: 

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v2
    - name: build
      run: |
        ./dapploy init test
        cd test
        yarn
        node_modules/.bin/ganache-cli > /dev/null &
        sleep 5
        ../dapploy -c .dapploy
